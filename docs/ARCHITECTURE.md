# Архитектура проекта Flat Earth Model v5

## Обзор

Flat Earth Model v5 использует многослойную модульную архитектуру, где каждый слой независим и взаимодействует с другими через чёткий API.

## Философия дизайна

### Принципы:
1. **Модульность** — каждый слой является самостоятельным модулем
2. **Независимость** — слои не зависят друг от друга напрямую
3. **Расширяемость** — легко добавлять новые слои
4. **Производительность** — GPU-ускорение через шейдеры
5. **Реалистичность** — максимальное приближение к наблюдаемой реальности

## Структура слоёв

```
Layer 4: Optics          ← Оптическая система (купол, линза)
         ↓
Layer 3: Dome            ← Купол и отражения
         ↓
Layer 2: Celestials      ← Светила (Солнце, Луна, звёзды)
         ↓
Layer 1: Atmosphere      ← Атмосфера и преломление
         ↓
Layer 0: Surface         ← Поверхность и освещение
```

## Layer 0: Surface (Поверхность)

**Статус:** ✅ Реализован (v5.0.0)

### Ответственность:
- Геометрия плоской Земли
- Реалистичное освещение от светил
- Расчёт световых пятен с градиентами
- День/ночь переходы

### Технологии:
- Three.js CircleGeometry (512 сегментов)
- Custom GLSL шейдеры
- GPU-ускоренные расчёты освещения

### API:
```javascript
// Создание слоя
const surface = new SurfaceLayer(scene, {
    earthRadius: 20000  // км
});

// Обновление освещения
surface.updateLighting(
    { x: 8000, y: 0 },      // Положение Солнца
    { x: -6000, y: 3000 }   // Положение Луны
);

// Получение яркости в точке
const brightness = surface.getBrightnessAt(x, y);  // 0.0 - 1.0

// Экспорт состояния для других слоёв
const state = surface.exportState();
```

### Входные данные:
- Положения светил (x, y координаты)
- Интенсивность источников света
- Радиусы световых пятен
- Параметры времени

### Выходные данные:
- Карта освещённости поверхности
- Положение подсолнечной точки
- Положение подлунной точки
- Яркость в любой точке поверхности

## Layer 1: Atmosphere (Атмосфера)

**Статус:** 📅 Запланирован (v5.1)

### Ответственность:
- Градиент плотности атмосферы
- Преломление света
- Рассеяние Рэлея (голубое небо)
- Сумеречные эффекты

### Технологии:
- Volumetric rendering
- Ray marching в шейдерах
- Физически корректное рассеяние

### API (проект):
```javascript
const atmosphere = new AtmosphereLayer(scene, {
    densityGradient: function(height) { ... },
    refractiveIndex: 1.000293
});

// Передача состояния от слоя Surface
atmosphere.update(surface.exportState());
```

## Layer 2: Celestial Bodies (Светила)

**Статус:** 📅 Запланирован (v5.2)

### Ответственность:
- 3D модели Солнца и Луны
- Орбитальная механика
- Затмения
- Фазы Луны
- Звёзды

## Layer 3: Dome (Купол)

**Статус:** 📅 Запланирован (v5.3)

### Ответственность:
- Зеркальная поверхность купола
- Отражения светил
- Фокусировка света
- Концентрация через атмосферу

## Layer 4: Optics (Оптическая система)

**Статус:** 📅 Запланирован (v6.0)

### Ответственность:
- Обратная трассировка лучей
- Линза в центре (магнитная гора)
- Полная оптическая модель
- Реконструкция положения светил

## Взаимодействие между слоями

### Схема передачи данных:

```javascript
// 1. Входные данные времени
const timeState = { year, month, day, hour, minute };

// 2. Расчёт астрономических позиций
const positions = calculateCelestialPositions(timeState);

// 3. Обновление Layer 0 (Surface)
surface.updateLighting(positions.sun, positions.moon);
const surfaceState = surface.exportState();

// 4. Обновление Layer 1 (Atmosphere) - будущее
atmosphere.update(surfaceState);
const atmosphereState = atmosphere.exportState();

// 5. Обновление Layer 2 (Celestials) - будущее
celestials.update(surfaceState, atmosphereState);

// И так далее...
```

### Контракт между слоями:

Каждый слой должен предоставлять:
1. **Constructor** — инициализация с конфигом
2. **update(state)** — обновление на основе состояния предыдущих слоёв
3. **exportState()** — экспорт своего состояния для следующих слоёв
4. **API методы** — для запросов данных

## Система управления временем

### TimeController

```javascript
class TimeController {
    constructor() {
        this.state = { year, month, day, hour, minute, second };
        this.speed = 1.0;  // Множитель скорости
        this.isRunning = false;
    }
    
    // Обновление времени
    update(deltaTime) {
        if (!this.isRunning) return;
        this.state.second += deltaTime * this.speed;
        this.normalizeTime();
    }
    
    // Получение положений светил
    getCelestialPositions() {
        return calculateCelestialPositions(this.state);
    }
}
```

## Астрономические расчёты

### Позиционирование Солнца

Упрощённая модель для Layer 0:

```javascript
function calculateSunPosition(timeState, earthRadius) {
    // День года (1-365)
    const dayOfYear = getDayOfYear(timeState);
    
    // Склонение Солнца
    const declination = -23.44 * Math.cos(
        2 * Math.PI * (dayOfYear + 10) / 365
    );
    
    // Часовой угол
    const hourAngle = (timeState.hour + timeState.minute / 60) / 24 * 2 * Math.PI;
    
    // Позиция на плоскости
    const distance = earthRadius * 0.4;  // 40% от радиуса
    return {
        x: distance * Math.cos(hourAngle),
        y: distance * Math.sin(hourAngle)
    };
}
```

### Позиционирование Луны

```javascript
function calculateMoonPosition(timeState, earthRadius) {
    const dayOfYear = getDayOfYear(timeState);
    const hourAngle = (timeState.hour + timeState.minute / 60) / 24 * 2 * Math.PI;
    
    // Лунный цикл (29.5 дней)
    const moonPhaseShift = dayOfYear * 0.0335;  // 2π / 29.5 / 6
    
    const distance = earthRadius * 0.3;  // 30% от радиуса
    return {
        x: distance * Math.cos(hourAngle + moonPhaseShift),
        y: distance * Math.sin(hourAngle + moonPhaseShift)
    };
}
```

## Система шейдеров

### Градиентное освещение

Каждый световой источник создаёт плавное пятно:

```glsl
// Fragment shader для Surface Layer
uniform vec2 sunPosition;
uniform vec2 moonPosition;
uniform float sunIntensity;
uniform float moonIntensity;
uniform float sunRadius;
uniform float moonRadius;

varying vec2 vPosition;

void main() {
    // Расстояние до подсолнечной точки
    float distToSun = length(vPosition - sunPosition);
    float sunBrightness = 0.0;
    
    if (distToSun < sunRadius) {
        float t = distToSun / sunRadius;
        sunBrightness = (1.0 - t) * sunIntensity;
        
        // Мягкий край
        if (t > 0.7) {
            sunBrightness *= (1.0 - (t - 0.7) / 0.3);
        }
    }
    
    // Аналогично для Луны...
    
    // Финальный цвет
    vec3 baseColor = vec3(0.1, 0.2, 0.3);  // Ночь
    vec3 sunColor = vec3(1.0, 0.96, 0.88) * sunBrightness;
    vec3 moonColor = vec3(0.69, 0.77, 0.87) * moonBrightness;
    
    gl_FragColor = vec4(baseColor + sunColor + moonColor, 1.0);
}
```

## Производительность

### Оптимизации:
1. **GPU шейдеры** — все расчёты освещения на GPU
2. **Единая геометрия** — один mesh для всей поверхности
3. **Минимум CPU** — только обновление uniform-переменных
4. **LOD** — Level of Detail для будущих слоёв

### Метрики (Layer 0):
- **FPS:** 60 (стабильные)
- **Полигоны:** 512 сегментов
- **Draw calls:** 1 на кадр
- **Память:** ~50 MB

## Будущие расширения

### v5.1 - Atmosphere Layer
- Объёмное рассеяние света
- Реалистичное голубое небо
- Сумерки и рассветы

### v5.2 - Celestial Bodies
- 3D модели светил
- Затмения
- Фазы Луны
- Звёздное небо

### v5.3 - Dome Layer
- Куполообразная структура
- Отражения и фокусировка
- Интеграция с атмосферой

### v6.0 - Full Optics
- Обратная трассировка
- Линза в центре (Рупес Нигра)
- Полная оптическая модель
- Реконструкция положений светил из наблюдений

## Заключение

Архитектура v5 создана с расчётом на постепенное расширение функциональности. Каждый новый слой добавляется независимо, не ломая существующий код.

Текущая реализация (Layer 0) является фундаментом для всех последующих слоёв и демонстрирует работоспособность концепции модульной архитектуры.
